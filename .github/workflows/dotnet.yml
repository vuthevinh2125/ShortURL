name: .NET CI - TinyUrlWebApp Build & Test

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  # Fix Lỗi Path/Extension: Sử dụng tên file đã được xác nhận trên GitHub
  SOLUTION_FILE: TinyUrlWebApp.slnx 
  DOTNET_VERSION: '8.0.x' 

jobs:
  build-and-test:
    # Fix Lỗi Case Sensitivity: Chuyển sang Windows Runner
    runs-on: windows-latest 

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      # Khôi phục gói NuGet
      run: dotnet restore ${{ env.SOLUTION_FILE }}

    # 🔥 Fix Lỗi CS0246 (xUnit): Kết hợp Build và Test
    - name: Build and Run Unit Tests
      # Lệnh này sẽ tự động Build (biên dịch) các dự án sau khi Restore, 
      # và chạy kiểm thử (test), hoàn tất quy trình CI
      run: dotnet test ${{ env.SOLUTION_FILE }} --configuration Release --verbosity normal
